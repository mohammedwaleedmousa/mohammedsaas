import {useState,type FormEvent}from'react';import{Link,useNavigate}from'react-router-dom';import{supabase}from'../lib/supabase.js';export function LoginPage(){const n=useNavigate(),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState(''),[busy,setBusy]=useState(false);async function submit(e:FormEvent){e.preventDefault();setBusy(true);setError('');const{error}=await supabase.auth.signInWithPassword({email,password});setBusy(false);if(error)setError('تعذر تسجيل الدخول. تحقق من البريد وكلمة المرور.');else n('/workspaces');}return <div className="auth-page"><div className="auth-card"><div className="auth-brand"><span className="brand-mark">M</span><div><strong>منصة الأعمال</strong><small>Business OS</small></div></div><h1>مرحبًا بعودتك</h1><p>سجّل الدخول للوصول إلى مساحة العمل.</p><form onSubmit={submit}><label>البريد الإلكتروني<input type="email" required value={email} onChange={e=>setEmail(e.target.value)}/></label><label>كلمة المرور<input type="password" required value={password} onChange={e=>setPassword(e.target.value)}/></label>{error&&<div className="form-error">{error}</div>}<button className="primary-button" disabled={busy}>{busy?'جارٍ الدخول…':'تسجيل الدخول'}</button><div className="auth-links"><Link to="/register">إنشاء حساب</Link><Link to="/forgot-password">نسيت كلمة المرور؟</Link></div></form></div><div className="auth-side"><div><span>Business Operating System</span><h2>المبيعات والمخزون والمحاسبة في دورة واحدة.</h2><p>أدخل العملية مرة واحدة، ودع النظام يسجل أثرها المالي والتشغيلي.</p></div></div></div>}
