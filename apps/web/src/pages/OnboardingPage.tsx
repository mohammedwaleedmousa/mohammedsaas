import{useMutation,useQueryClient}from'@tanstack/react-query';import{useState,type FormEvent}from'react';import{useNavigate}from'react-router-dom';import{api}from'../lib/api.js';export function OnboardingPage(){const n=useNavigate(),qc=useQueryClient(),[name,setName]=useState(''),[branch,setBranch]=useState('الفرع الرئيسي'),[currency,setCurrency]=useState('SAR'),[country,setCountry]=useState('YE'),m=useMutation({mutationFn:()=>api<{slug:string}>('/workspaces',{method:'POST',body:JSON.stringify({name,branchName:branch,baseCurrency:currency,country,timezone:'Asia/Aden',language:'ar'})}),onSuccess:async d=>{await qc.invalidateQueries({queryKey:['workspaces']});n(`/app/${d.slug}`);}});function submit(e:FormEvent){e.preventDefault();m.mutate();}return <div className="onboarding-page"><form className="setup-card" onSubmit={submit}><span className="eyebrow">إعداد سريع</span><h1>أنشئ شركتك</h1><label>اسم الشركة<input required value={name} onChange={e=>setName(e.target.value)}/></label><div className="form-grid"><label>الدولة<select value={country} onChange={e=>setCountry(e.target.value)}><option value="YE">اليمن</option><option value="SA">السعودية</option><option value="AE">الإمارات</option></select></label><label>العملة<select value={currency} onChange={e=>setCurrency(e.target.value)}><option>SAR</option><option>YER</option><option>USD</option><option>AED</option></select></label></div><label>الفرع الأول<input required value={branch} onChange={e=>setBranch(e.target.value)}/></label><button className="primary-button" disabled={m.isPending}>{m.isPending?'جارٍ الإنشاء…':'إنشاء الشركة وبدء التجربة'}</button></form></div>}
