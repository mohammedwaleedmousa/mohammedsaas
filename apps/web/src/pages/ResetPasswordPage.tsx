import{useState,type FormEvent}from'react';import{useNavigate}from'react-router-dom';import{supabase}from'../lib/supabase.js';export function ResetPasswordPage(){const[p,setP]=useState(''),n=useNavigate(),[err,setErr]=useState('');async function submit(x:FormEvent){x.preventDefault();const{error}=await supabase.auth.updateUser({password:p});if(error)setErr('تعذر تحديث كلمة المرور.');else n('/workspaces');}return <div className="screen-center"><form className="setup-card" onSubmit={submit}><h1>كلمة مرور جديدة</h1><label>كلمة المرور<input type="password" minLength={6} required value={p} onChange={x=>setP(x.target.value)}/></label>{err&&<div className="form-error">{err}</div>}<button className="primary-button">حفظ</button></form></div>}
