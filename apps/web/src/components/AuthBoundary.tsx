import {useEffect,useState,type ReactNode} from 'react';import {Navigate,useLocation} from 'react-router-dom';import type {Session} from '@supabase/supabase-js';import {supabase} from '../lib/supabase.js';export function AuthBoundary({children}:{children:ReactNode}){const[s,setS]=useState<Session|null|undefined>();const l=useLocation();useEffect(()=>{void supabase.auth.getSession().then(({data})=>setS(data.session));const{data}=supabase.auth.onAuthStateChange((_e,n)=>setS(n));return()=>data.subscription.unsubscribe();},[]);if(s===undefined)return <div className="screen-center"><div className="loader"/></div>;if(!s)return <Navigate to="/login" replace state={{from:l.pathname}}/>;return children;}
