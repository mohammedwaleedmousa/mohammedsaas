import {CanActivate,ExecutionContext,ForbiddenException,Injectable} from '@nestjs/common';import type {AuthenticatedRequest} from '../auth/request-context.js';const W=new Set(['TRIAL','ACTIVE','PAST_DUE','GRACE_PERIOD']);@Injectable()export class SubscriptionWriteGuard implements CanActivate{canActivate(c:ExecutionContext){const r=c.switchToHttp().getRequest<AuthenticatedRequest>();if(['GET','HEAD','OPTIONS'].includes(r.method))return true;const s=r.tenantContext?.subscriptionState;if(!s)return true;if(!W.has(s))throw new ForbiddenException('Subscription is read-only or suspended');return true;}}
