import {createParamDecorator,ExecutionContext} from '@nestjs/common';import type {TenantContext} from '@mohammedsaas/types';import type {AuthenticatedRequest} from '../auth/request-context.js';export const CurrentTenant=createParamDecorator((_d:unknown,c:ExecutionContext):TenantContext=>{const r=c.switchToHttp().getRequest<AuthenticatedRequest>();if(!r.tenantContext)throw new Error('Tenant context missing');return r.tenantContext;});
