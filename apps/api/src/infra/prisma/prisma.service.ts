import {Injectable,OnModuleDestroy} from '@nestjs/common';import {PrismaPg} from '@prisma/adapter-pg';import {PrismaClient,type Prisma} from '../../generated/prisma/client.js';@Injectable()export class PrismaService extends PrismaClient implements OnModuleDestroy{constructor(){const c=process.env.DATABASE_URL;if(!c)throw new Error('DATABASE_URL is required');super({adapter:new PrismaPg({connectionString:c})});}async onModuleDestroy(){await this.$disconnect();}async forTenant<T>(tenantId:string,work:(tx:Prisma.TransactionClient)=>Promise<T>):Promise<T>{return this.$transaction(async tx=>{await tx.$executeRaw`SELECT set_config('app.tenant_id', ${tenantId}, true)`;return work(tx);},{isolationLevel:'Serializable',maxWait:5000,timeout:15000});}}
