import 'reflect-metadata';import 'dotenv/config';import helmet from 'helmet';import {NestFactory} from '@nestjs/core';import {AppModule} from './app.module.js';import {HttpExceptionFilter} from './common/http-exception.filter.js';const required=(names:string[])=>{const m=names.filter(n=>!process.env[n]);if(m.length)throw new Error(`Missing required environment variables: ${m.join(', ')}`);};async function bootstrap(){required(['DATABASE_URL','SUPABASE_JWKS_URL','SUPABASE_JWT_ISSUER','WEB_ORIGIN']);const app=await NestFactory.create(AppModule,{bufferLogs:true});app.setGlobalPrefix('api/v1');app.use(helmet({contentSecurityPolicy:false}));app.enableCors({origin:process.env.WEB_ORIGIN!.split(',').map(x=>x.trim()),credentials:false,methods:['GET','POST','PUT','PATCH','DELETE','OPTIONS'],allowedHeaders:['Authorization','Content-Type','Idempotency-Key']});app.useGlobalFilters(new HttpExceptionFilter());app.enableShutdownHooks();await app.listen(Number(process.env.PORT??4000),'0.0.0.0');}void bootstrap();
