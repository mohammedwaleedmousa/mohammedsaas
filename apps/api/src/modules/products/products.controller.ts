import {Body,Controller,Get,Post,Query} from '@nestjs/common';import {z} from 'zod';import {PERMISSIONS} from '@mohammedsaas/permissions';import type {TenantContext} from '@mohammedsaas/types';import {CurrentTenant} from '../../common/tenant/tenant-context.decorator.js';import {RequirePermissions} from '../../common/permissions/permissions.decorator.js';import {ProductsService} from './products.service.js';const schema=z.object({sku:z.string().min(1).max(80),barcode:z.string().max(120).optional(),name:z.string().min(2).max(180),unit:z.string().max(40).default('piece'),costPrice:z.string().regex(/^\d+(\.\d{1,4})?$/),sellingPrice:z.string().regex(/^\d+(\.\d{1,4})?$/),taxRate:z.string().regex(/^\d+(\.\d{1,4})?$/).default('0.0000'),minimumStock:z.string().regex(/^\d+(\.\d{1,4})?$/).default('0.0000'),trackInventory:z.boolean().default(true)});@Controller('workspaces/:workspaceSlug/products')export class ProductsController{constructor(private readonly s:ProductsService){}@Get()@RequirePermissions(PERMISSIONS.PRODUCTS_VIEW)list(@CurrentTenant()c:TenantContext,@Query('q')q=''){return this.s.list(c,q);}@Post()@RequirePermissions(PERMISSIONS.PRODUCTS_CREATE)create(@CurrentTenant()c:TenantContext,@Body()b:unknown){return this.s.create(c,schema.parse(b));}}
