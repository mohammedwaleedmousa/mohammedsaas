import {Body,Controller,Get,Post} from '@nestjs/common';import {z} from 'zod';import {PERMISSIONS} from '@mohammedsaas/permissions';import type {TenantContext} from '@mohammedsaas/types';import {CurrentTenant} from '../../common/tenant/tenant-context.decorator.js';import {RequirePermissions} from '../../common/permissions/permissions.decorator.js';import {BranchesService} from './branches.service.js';const b=z.object({name:z.string().min(2).max(140),code:z.string().min(1).max(30)}),w=z.object({branchId:z.string().uuid(),name:z.string().min(2).max(140),code:z.string().min(1).max(30)});@Controller('workspaces/:workspaceSlug/branches')export class BranchesController{constructor(private readonly s:BranchesService){}@Get()@RequirePermissions(PERMISSIONS.BRANCHES_VIEW)list(@CurrentTenant()c:TenantContext){return this.s.list(c);}@Post()@RequirePermissions(PERMISSIONS.BRANCHES_MANAGE)create(@CurrentTenant()c:TenantContext,@Body()x:unknown){return this.s.createBranch(c,b.parse(x));}@Post('warehouses')@RequirePermissions(PERMISSIONS.BRANCHES_MANAGE)warehouse(@CurrentTenant()c:TenantContext,@Body()x:unknown){return this.s.createWarehouse(c,w.parse(x));}}
