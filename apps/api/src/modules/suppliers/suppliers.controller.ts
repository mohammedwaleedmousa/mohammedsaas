import {Body,Controller,Get,Post,Query} from '@nestjs/common';import {z} from 'zod';import {PERMISSIONS} from '@mohammedsaas/permissions';import type {TenantContext} from '@mohammedsaas/types';import {CurrentTenant} from '../../common/tenant/tenant-context.decorator.js';import {RequirePermissions} from '../../common/permissions/permissions.decorator.js';import {SuppliersService} from './suppliers.service.js';const schema=z.object({name:z.string().min(2).max(180),phone:z.string().max(40).optional(),email:z.string().email().optional(),taxNumber:z.string().max(80).optional(),address:z.string().max(500).optional()});@Controller('workspaces/:workspaceSlug/suppliers')export class SuppliersController{constructor(private readonly s:SuppliersService){}@Get()@RequirePermissions(PERMISSIONS.SUPPLIERS_VIEW)list(@CurrentTenant()c:TenantContext,@Query('q')q=''){return this.s.list(c,q);}@Post()@RequirePermissions(PERMISSIONS.SUPPLIERS_CREATE)create(@CurrentTenant()c:TenantContext,@Body()b:unknown){return this.s.create(c,schema.parse(b));}}
