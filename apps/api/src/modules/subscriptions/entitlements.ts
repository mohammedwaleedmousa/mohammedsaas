export type Entitlements=Record<string,boolean>;export function resolveEntitlements(plan:unknown,overrides:Array<{key:string;enabled:boolean}>=[]):Entitlements{const out:Entitlements={};if(plan&&typeof plan==='object'&&!Array.isArray(plan))for(const [k,v] of Object.entries(plan))out[k]=v===true;for(const o of overrides)out[o.key]=o.enabled;return out;}export const canUse=(e:Entitlements,key:string)=>e[key]===true;
