import {Controller,Get} from '@nestjs/common';import {PERMISSIONS} from '@mohammedsaas/permissions';import type {TenantContext} from '@mohammedsaas/types';import {CurrentTenant} from '../../common/tenant/tenant-context.decorator.js';import {RequirePermissions} from '../../common/permissions/permissions.decorator.js';import {SubscriptionsService} from './subscriptions.service.js';@Controller('workspaces/:workspaceSlug/subscription')export class SubscriptionsController{constructor(private readonly s:SubscriptionsService){}@Get()@RequirePermissions(PERMISSIONS.SUBSCRIPTION_VIEW)get(@CurrentTenant()c:TenantContext){return this.s.get(c.tenantId);}}
