export type BillingActivation=Readonly<{externalReference?:string;paidThrough?:Date}>;export interface BillingProviderAdapter{readonly key:'MANUAL'|'STRIPE'|'REGIONAL';activate(input:{tenantId:string;planCode:string;amount:string;currency:string}):Promise<BillingActivation>;}export class ManualBillingProvider implements BillingProviderAdapter{readonly key='MANUAL' as const;async activate():Promise<BillingActivation>{return{};}}
