import {Body,Controller,Get,Post} from '@nestjs/common';import {z} from 'zod';import {PERMISSIONS} from '@mohammedsaas/permissions';import type {TenantContext} from '@mohammedsaas/types';import {CurrentTenant} from '../../common/tenant/tenant-context.decorator.js';import {RequirePermissions} from '../../common/permissions/permissions.decorator.js';import {ExpensesService} from './expenses.service.js';const dec=z.string().regex(/^\d+(\.\d{1,4})?$/),schema=z.object({branchId:z.string().uuid(),categoryId:z.string().uuid(),amount:dec,tax:dec.default('0.0000'),paymentMethod:z.enum(['CASH','CARD','BANK_TRANSFER','OTHER']),accountCode:z.string().max(20).optional(),vendor:z.string().max(180).optional(),description:z.string().max(500).optional(),occurredAt:z.coerce.date().default(()=>new Date())});@Controller('workspaces/:workspaceSlug/expenses')export class ExpensesController{constructor(private readonly s:ExpensesService){}@Get('categories')@RequirePermissions(PERMISSIONS.EXPENSES_VIEW)categories(@CurrentTenant()c:TenantContext){return this.s.categories(c);}@Get()@RequirePermissions(PERMISSIONS.EXPENSES_VIEW)list(@CurrentTenant()c:TenantContext){return this.s.list(c);}@Post()@RequirePermissions(PERMISSIONS.EXPENSES_CREATE)create(@CurrentTenant()c:TenantContext,@Body()b:unknown){return this.s.create(c,schema.parse(b));}}
