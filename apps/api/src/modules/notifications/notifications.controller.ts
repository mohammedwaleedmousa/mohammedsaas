import {Controller,Get,Param,Post} from '@nestjs/common';import type {TenantContext} from '@mohammedsaas/types';import {CurrentTenant} from '../../common/tenant/tenant-context.decorator.js';import {NotificationsService} from './notifications.service.js';@Controller('workspaces/:workspaceSlug/notifications')export class NotificationsController{constructor(private readonly s:NotificationsService){}@Get()list(@CurrentTenant()c:TenantContext){return this.s.list(c);}@Post(':id/read')read(@CurrentTenant()c:TenantContext,@Param('id')id:string){return this.s.read(c,id);}}
