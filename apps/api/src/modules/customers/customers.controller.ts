import {Body,Controller,Get,Post,Query} from '@nestjs/common';import {z} from 'zod';import {PERMISSIONS} from '@mohammedsaas/permissions';import type {TenantContext} from '@mohammedsaas/types';import {CurrentTenant} from '../../common/tenant/tenant-context.decorator.js';import {RequirePermissions} from '../../common/permissions/permissions.decorator.js';import {CustomersService} from './customers.service.js';const schema=z.object({name:z.string().min(2).max(180),phone:z.string().max(40).optional(),email:z.string().email().optional(),taxNumber:z.string().max(80).optional(),address:z.string().max(500).optional(),creditLimit:z.string().regex(/^\d+(\.\d{1,4})?$/).default('0.0000')});@Controller('workspaces/:workspaceSlug/customers')export class CustomersController{constructor(private readonly s:CustomersService){}@Get()@RequirePermissions(PERMISSIONS.CUSTOMERS_VIEW)list(@CurrentTenant()c:TenantContext,@Query('q')q='',@Query('page')p='1'){return this.s.list(c,q,Number(p));}@Post()@RequirePermissions(PERMISSIONS.CUSTOMERS_CREATE)create(@CurrentTenant()c:TenantContext,@Body()b:unknown){return this.s.create(c,schema.parse(b));}}
