import {CanActivate,ExecutionContext,ForbiddenException,Injectable} from '@nestjs/common';import type {AuthenticatedRequest} from '../../common/auth/request-context.js';@Injectable()export class PlatformAdminGuard implements CanActivate{canActivate(c:ExecutionContext){const r=c.switchToHttp().getRequest<AuthenticatedRequest>(),allowed=new Set((process.env.PLATFORM_ADMIN_AUTH_IDS??'').split(',').map(x=>x.trim()).filter(Boolean));if(!r.principal||!allowed.has(r.principal.authUserId))throw new ForbiddenException('Platform administrator access required');return true;}}
