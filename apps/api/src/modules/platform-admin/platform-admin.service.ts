import {BadRequestException,Injectable,NotFoundException} from '@nestjs/common';import {addDecimals} from '@mohammedsaas/accounting-engine';import {PrismaService} from '../../infra/prisma/prisma.service.js';type A={action:'ACTIVATE';planCode:string;months:number}|{action:'EXTEND';days:number}|{action:'START_TRIAL';days:number}|{action:'CHANGE_PLAN';planCode:string}|{action:'MARK_PAYMENT';amount:string;currency:string;externalRef?:string}|{action:'SUSPEND'};@Injectable()export class PlatformAdminService{constructor(private readonly p:PrismaService){}async overview(){const [totalCompanies,activeCompanies,trialCompanies,activeSubscriptions,activeUsers,subs]=await Promise.all([this.p.tenant.count(),this.p.subscription.count({where:{status:'ACTIVE'}}),this.p.subscription.count({where:{status:'TRIAL'}}),this.p.subscription.count({where:{status:'ACTIVE'}}),this.p.user.count(),this.p.subscription.findMany({where:{status:'ACTIVE'},include:{plan:true}})]);const mrrByCurrency:Record<string,string>={};for(const s of subs)mrrByCurrency[s.plan.currency]=addDecimals(mrrByCurrency[s.plan.currency]??'0.0000',s.plan.monthlyPrice.toString());return{totalCompanies,activeCompanies,trialCompanies,activeSubscriptions,activeUsers,mrrByCurrency};}companies(){return this.p.tenant.findMany({select:{id:true,name:true,slug:true,createdAt:true,subscription:{include:{plan:true}},_count:{select:{memberships:true,branches:true}}},orderBy:{createdAt:'desc'},take:100});}subscriptionAction(actor:string,tenantId:string,input:A){return this.p.$transaction(async tx=>{const s=await tx.subscription.findUnique({where:{tenantId},include:{plan:true}});if(!s)throw new NotFoundException();const now=new Date();if(input.action==='MARK_PAYMENT')await tx.billingPayment.create({data:{subscriptionId:s.id,provider:'MANUAL',amount:input.amount,currency:input.currency,receivedAt:now,...(input.externalRef?{externalRef:input.externalRef}:{})}});let data={};if(input.action==='SUSPEND')data={status:'SUSPENDED' as const};else if(input.action==='START_TRIAL')data={status:'TRIAL' as const,trialEndsAt:new Date(now.getTime()+input.days*86400000)};else if(input.action==='EXTEND'){const base=s.currentPeriodEndsAt&&s.currentPeriodEndsAt>now?s.currentPeriodEndsAt:now;data={status:'ACTIVE' as const,currentPeriodEndsAt:new Date(base.getTime()+input.days*86400000)};}else if(input.action==='ACTIVATE'||input.action==='CHANGE_PLAN'){const plan=await tx.subscriptionPlan.findUnique({where:{code:input.planCode}});if(!plan)throw new BadRequestException('Plan not found');data={planId:plan.id,...(input.action==='ACTIVATE'?{status:'ACTIVE' as const,currentPeriodEndsAt:new Date(now.getTime()+input.months*30*86400000)}:{})};}else if(input.action==='MARK_PAYMENT')data={status:'ACTIVE' as const};const out=await tx.subscription.update({where:{tenantId},data});await tx.subscriptionEvent.create({data:{subscriptionId:s.id,type:input.action,metadata:input}});await tx.auditLog.create({data:{tenantId,actorId:actor,action:`platform_subscription_${input.action.toLowerCase()}`,entityType:'subscription',entityId:s.id,metadata:input}});return out;});}}
